#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns the length of a string.
 * @s: void value add
 * return 0
 */
int _strlen(char *s)
{
char i;
i = strlen(s);
return (i);
}
